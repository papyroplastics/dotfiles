
ctrl + alt + t 
    kitty

super + t
    kitty

super + {e,w}
    {nemo,firefox}

super + {r,f,Tab}
    rofi -show {drun,filebrowser,window}

super + l
   	bspc desktop -l next

alt + Tab
   	bspc node -f next

alt + shift + q
    bspc quit

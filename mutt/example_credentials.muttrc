set my_email_at = ""
set my_service = ""
set my_username = ""
set my_fullname = ""

# password must be stored in the system keyring
set my_pass_cmd = (concat 'secret-tool lookup application mutt username ' $my_username ' service ' $my_service)
run (concat 'set my_password = "`' $my_pass_cmd '`"' )
